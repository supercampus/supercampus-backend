openapi: 3.1.0
info:
  title: SuperCampus Academics API
  version: 0.1.0
paths:
  /api/v1/academics:
    get:
      operationId: listAcademicsModule
      responses:
        "200": { description: Tenant-filtered Academics records }