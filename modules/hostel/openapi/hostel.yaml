openapi: 3.1.0
info:
  title: SuperCampus Hostel API
  version: 0.1.0
paths:
  /api/v1/hostel:
    get:
      operationId: listHostelModule
      responses:
        "200": { description: Tenant-filtered Hostel records }